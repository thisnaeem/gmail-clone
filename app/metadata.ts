import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Gmail Viewer',
  description: 'A modern Gmail viewer application',
  viewport: 'width=device-width, initial-scale=1',
}; 